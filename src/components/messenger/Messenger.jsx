/**
 * 이 컴포넌트는 이제 비어있습니다.
 * 메신저 UI는 기존 messenger.js 가 직접 document.body 에 주입합니다.
 * (Floating button, Modal, Notifications 등 전부)
 * 
 * Layout 에서 이 컴포넌트를 마운트하긴 하지만 사실 아무 일도 안 합니다.
 * 추후 React 컴포넌트로 점진적으로 재작성할 때 이 자리에 들어갑니다.
 */
export default function Messenger() {
  return null;
}